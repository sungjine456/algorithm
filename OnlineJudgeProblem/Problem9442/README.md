## Problem : 9442(Sort Me)

---
### 풀이 : 
#### 정렬 순서를 입력받은 후 차례대로 Map에 Key로 넣으며 Value는 0부터 1씩 더해서 넣는다.
#### 그다음 정렬해야하는 문자열을 배열에 넣고 Collections로 정렬한다.
#### Comparator을 통해서 재정의 하면 되는데 문자열을 받아 char형 배열로 변환한 후 차례대로 비교하면 된다.